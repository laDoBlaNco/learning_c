/**
 * First C++ Programm
 */

 #include <iostream>

using namespace std;

int main(int argc, char const *argv[]) // also not necessary, but we can get input from terminal
// with this, if we ever want to
{
    cout << "Hello World" << endl; // the endl is just adding a newline, also optional

    return 0; // not necessary anymore really since recent versions
}